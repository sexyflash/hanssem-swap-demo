=== [RETRY ANALYSIS · stage 1] ===
diagnosis: 직전 결과는 컬러명 텍스트를 아예 넣지 않아 핵심 검증 항목이 빠졌고, 하단 좌측 칸을 제거·크롭해야 하는 지시가 충분히 강제되지 않아 4칸 구도/빈칸 잔존으로 단일 패널 판정이 흔들렸습니다. 라벨 배치 위치·표기 문자열·타이포 규격이 구체화되지 않아 ‘정확히 3가지 컬러명’ 표기를 놓친 것이 근본 원인입니다.
patches:
  - 컬러 라벨을 하드 앵커로 강제: 각 스와치 안 하단 중앙에 정확히 ‘화이트 하이글로시(유광)’, ‘크림화이트’, ‘라이트오크’ 텍스트를 배치하도록 명시(철자·괄호 포함, 다른 텍스트 금지).
  - 3스와치 레이아웃을 강제: 하단 좌측 칸은 실제로 제거하고 캔버스를 그 영역만큼 크롭하여 L자 3칸만 남기도록 단계형 지시와 체크리스트 추가(빈 흰칸·테두리 잔존 금지).
  - 단일 패널 고정: ‘한 장의 합성 이미지’만 출력, 추가 패널/썸네일/보더 금지 문구를 상단에 명시.
  - 손잡이 톤 앵커: 상단 좌/우(크림화이트·화이트 유광)는 동일 톤 손잡이, 라이트오크는 한 톤 짙은 브라운으로 명시.
  - 컬러 톤 매칭 우선순위를 레퍼런스 파일로 고정하고, 없을 때의 톤 가이드 보조 규칙 유지.
  - 최종 점검 체크리스트를 추가해 라벨 3종 정확 표기, 3스와치, 단일 패널, 빈칸 없음 여부를 출고 전 자가 검증.

=== [★ PRODUCT IDENTITY · 절대 왜곡되면 안 됨] ===
(이 항목들은 QA가 binary 검증. 1개라도 fail이면 retry.)

  ◆ [DISTORTION] 컬러 옵션 패널에 정확히 3가지 도어 컬러명이 표기됨. (axis=color)
     → 정답: 화이트 하이글로시(유광) / 크림화이트 / 라이트오크

  (참고용 · detail — 시도하되 strict 채점 안 함):
     · 손잡이와 도어는 서로 다른 소재/톤으로 색 대비가 보임(크림화이트 도어도 동일 손잡이 톤). (axis=handles)
     · 컬러 옵션에서 크림화이트 도어의 손잡이 컬러가 화이트 도어와 동일 톤으로 표현됨. (axis=handles)

TASK: 한국 디테일페이지용 컬러 옵션 패널을 사용자 제품으로 스왑하라.

CRITICAL MUST-PASS (출력 이미지에 반드시 충족):
- 단일 패널 1장만 출력(한 장의 합성 이미지). 추가 패널/썸네일/프레임/보더/여러 장 콜라주 금지.
- 3스와치 구성: 상단 좌, 상단 우, 하단 우 3칸만 사용. 하단 좌측 칸은 제거하고 해당 영역만큼 캔버스를 크롭해 L자 레이아웃으로 재정렬(기존 그리드 간격/여백 비율 유지, 빈칸·흰 영역 잔존 금지).
- 컬러명 텍스트 3가지를 정확히 표기(철자·괄호 그대로): 상단 좌=크림화이트, 상단 우=화이트 하이글로시(유광), 하단 우=라이트오크. 다른 텍스트·아이콘 추가 금지.

REFERENCE PATTERN (image_urls 순서 — panel별 LLM 매핑):
- IMAGE 1 = panel C (한샘 원본 — edit base, 레이아웃 감각 유지)
- IMAGE 2 = SMART SHEET (메타 가이드 — 결과 반영 X, 정보만 참고)
- IMAGE 3 = USER PRODUCT — screenshot_01.png
- IMAGE 4 = USER PRODUCT — screenshot_03.png
- IMAGE 5 = USER PRODUCT — screenshot_04.png
- IMAGE 6 = USER PRODUCT — screenshot_10.png

USER PRODUCT: 샘베딩 클로즈 옷장세트 160cm(높이216cm) 서랍형 6종
주요 스펙/디테일(사실 그대로 유지):
- 사이즈: 160×57×216cm, 모듈 조합형(행거/서랍/선반/거울), 높이 216cm 라인업(본 상품).
- 도어/손잡이: 세로형 매립 손잡이. 크림화이트 도어의 손잡이는 화이트 도어와 동일 톤. 손잡이는 목분(우드 컴포지트) 질감.
- 힌지/내부: 댐핑 힌지(부드럽게 닫힘), 선반 18mm, 깊이 57cm, 행거봉 최대 약 16kg(80cm 기준), E0 등급 소재.

SPATIAL LAYOUT TO PRESERVE(패널 C의 카메라·구도 감각 유지):
- 원본은 2×2 표면 클로즈업이지만 이번 결과는 3스와치(L자). 상단 좌/우, 하단 우 세 칸의 크롭·손잡이 중앙 정렬·여백 비율은 원본 감각에 맞추되, 제거된 하단 좌측 칸은 캔버스에서 완전히 제외(빈칸 금지).
- 각 스와치 중앙에 세로 매립 손잡이 유지. 유광/무광에 따른 반사만 조정.

EXPLICIT EDITS — color_option:
1) 레이아웃/크롭
- 하단 좌측 칸을 삭제하고 그 영역만큼 캔버스를 왼쪽 또는 아래 방향으로 크롭하여 정방형에 가까운 L자 3칸 레이아웃 완성.
- 그리드 간격/여백은 상하·좌우 동일 비율로 유지. 외곽 테두리선, 구획선 새로 추가 금지.

2) 컬러 매핑 및 표면 표현
- 상단 좌=크림화이트: 미색 톤의 무광 화이트. 따뜻한 아이보리(YR 미세 틴트), 난반사로 매트 질감. 손잡이 톤은 상단 우와 동일.
- 상단 우=화이트 하이글로시(유광): 순백에 가까운 화이트. 강한 대각 하이라이트와 반광택 그라데이션으로 유광 강조. 손잡이 톤은 상단 좌와 동일.
- 하단 우=라이트오크: 밝은 내추럴 오크. 수직 스트레이트 그레인, 대비는 과하지 않게. 손잡이는 우드와 구분되는 한 톤 짙은 브라운.

3) 손잡이 디테일(공통)
- 형태: 도어를 따라 길게 내려오는 매립형 세로 손잡이 유지.
- 소재감: 금속 느낌 배제, 매트한 목분 질감.
- 톤 규칙 앵커: 상단 좌/우(두 화이트계)는 동일 손잡이 톤. 라이트오크는 한 톤 더 짙은 브라운.

4) 컬러 라벨(텍스트) — 하드 앵커
- 각 스와치 내부 하단 중앙에 캡션 형태로 배치. 정확 표기:
  • 상단 좌: 크림화이트
  • 상단 우: 화이트 하이글로시(유광)
  • 하단 우: 라이트오크
- 타이포: 가독성 높은 한국어 산세리프, Bold 600 전후, 자간 0~2%, 글자색 #111(밝은 배경 위). 우드처럼 배경 무늬가 있는 경우 캡션 뒤에 80~90% 흰색 반투명 라벨칩(라운드 코너) 추가하여 선명도 확보. 오탈자/문구 변형/영문 금지.

5) 조명/카메라/구도
- 원본 패널의 카메라 각도, 스와치 내 손잡이 중심 위치, 여백 비율 유지. 유광/무광 차이에 따른 반사/하이라이트만 조정.

6) 레퍼런스 매칭
- 컬러 최종 톤은 제공 레퍼런스(screenshot_01.png, screenshot_03.png, screenshot_04.png, screenshot_10.png)에 동일 컬러가 보이면 이를 최우선 매칭.
- 부재 시 가이드: 화이트 하이글로시(유광)=가장 밝은 N계 화이트, 크림화이트=따뜻한 아이보리(YR 소폭 틴트), 라이트오크=밝은 내추럴 오크(옅은 베이지 우드).

ABSOLUTE RULES — PRESERVE / NEVER
PRESERVE:
- 단일 패널 레이아웃(한국 디테일페이지 스타일), 카메라 각도/배경/조명/한국어 텍스트 가독성.
- 사용자 제품의 실제 특징 및 위 손잡이/컬러 톤 규칙.
NEVER:
- 멀티 패널/썸네일 모음/여러 장 콜라주/보더 추가.
- 원본 한샘 텍스트 잔존.
- 한글 텍스트 깨짐/오탈자.
- 존재하지 않는 기능 발명.

FINAL CHECKLIST(출력 직전 자가 검증):
- [ ] 단일 이미지 1장인가?
- [ ] 하단 좌측 칸이 완전 제거되어 3스와치(L자)만 보이고 빈칸 없음?
- [ ] 컬러 라벨이 정확히 3개, 정확 문구로 배치되었는가(크림화이트 / 화이트 하이글로시(유광) / 라이트오크)?
- [ ] 상단 좌·우 손잡이 톤 동일, 라이트오크 손잡이는 한 톤 짙은 브라운인가?
- [ ] 유광/무광 질감 표현과 하이라이트 방향이 자연스러운가?
